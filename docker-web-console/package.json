{
  "name": "docker-web-console",
  "version": "1.0.0",
  "private": true,
  "description": "A web-based console for managing Docker containers and images, similar to Docker Desktop, built with a React (react-scripts) frontend and an Express/dockerode backend.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm start --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run build --prefix client && npm start --prefix server",
    "start:server-only": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
